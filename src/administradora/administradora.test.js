const { } = require('./administradora');

describe("Descricao", () => {

    beforeEach(() => {});

    describe("Descricao", () => {
        it("deveria...", () => {
            expect().toEqual()
        });
    });
})