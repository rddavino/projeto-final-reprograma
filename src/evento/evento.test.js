const { } = require('./evento');

describe("Descricao", () => {

    beforeEach(() => {});

    describe("Descricao", () => {
        it("deveria...", () => {
            expect().toEqual()
        });
    });
})