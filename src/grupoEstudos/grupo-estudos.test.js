const { } = require('./grupo-estudos');

describe("Descricao", () => {

    beforeEach(() => {});

    describe("Descricao", () => {
        it("deveria...", () => {
            expect().toEqual()
        });
    });
})